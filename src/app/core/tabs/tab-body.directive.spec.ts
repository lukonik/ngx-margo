import { TabBodyDirective } from './tab-body.directive';

describe('TabBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new TabBodyDirective();
    expect(directive).toBeTruthy();
  });
});
